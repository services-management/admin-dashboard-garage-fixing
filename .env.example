# Development API base forwarded by Vite dev proxy
# In development set this to `/api` so requests are proxied to the real backend
VITE_API_BASE=/api

# For production set the real API host, e.g.:
# VITE_API_BASE=https://garas-api.itedev.online
